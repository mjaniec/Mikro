#ifndef GP_GESTURES_H_
#define GP_GESTURES_H_

struct gpGesture {
	gpInt code;
	union info{
		struct {				// positon
			gpFloat x;
			gpFloat y;
		};
		gpBool in;
		gpBool out;
	};
};

#endif /* GP_GESTURES_H_ */
