SUBDIRS = Base Math Alloc InstanceLearner

all: ${subdirs}
	for dir in $(SUBDIRS); do \
		$(MAKE) $@ -C $$dir; \
	done
   
clean: ${subdirs}
	for dir in $(SUBDIRS); do \
		$(MAKE) $@ -C $$dir; \
	done
                       