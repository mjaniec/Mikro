CC=gcc
CFLAGS=-I"${INCLUDE}/Base" -I"${INCLUDE}/Math" -I"${INCLUDE}/Alloc" -D${MODE} -std=c99  -Wno-main -Wall -lGPBase -lGPMath

all: clean libGPAlloc.a
clean:;	rm -f *.o *.exe *.a

libGPAlloc.a: gp_Alloc.o
	ar -r ${OUTPUT}\$@ $^
	
%.o:%.c
	gcc -c ${CFLAGS} -o $@ $<
