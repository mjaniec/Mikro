SUBDIRS ?= Include Source

all: ${subdirs}
	for dir in $(SUBDIRS); do \
		$(MAKE) $@ -C $$dir; \
	done
   
clean: ${subdirs}
	mkdir ${OUTPUT}
	for dir in $(SUBDIRS); do \
		$(MAKE) $@ -C $$dir; \
	done
	rm -rf Output
                       